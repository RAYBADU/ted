{
  "private": true,
  "scripts": {
    "build": "npm --prefix ted run build",
    "deploy": "npm --prefix ted run deploy",
    "predeploy": "npm --prefix ted run predeploy",
    "dev": "npm --prefix ted run dev",
    "preview": "npm --prefix ted run preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "framer-motion": "^12.38.0",
    "react-icons": "^5.6.0",
    "react-router-dom": "^7.14.1",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
